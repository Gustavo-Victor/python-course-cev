def increase(price=1, percentage=10): 
    return price * (1 + (percentage / 100));
    
def decrease(price=1, percentage=10): 
    return price * (1 - (percentage / 100));
        
def double(price=1): 
    return price * 2;

def half(price=1): 
    return price / 2;


